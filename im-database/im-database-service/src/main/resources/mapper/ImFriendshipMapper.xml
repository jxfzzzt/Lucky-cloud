<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xy.lucky.database.web.mapper.ImFriendshipMapper">

    <select id="selectFriendList" resultType="com.xy.lucky.domain.po.ImFriendshipPo">
        select owner_id,
               to_id,
               remark,
               black,
               sequence,
               black_sequence,
               add_source,
               extra,
               create_time,
               update_time,
               del_flag,
               version
        from im_friendship
        <where>
            <if test="ownerId != null and ownerId != ''">
                AND owner_id = #{ownerId}
            </if>
            <if test="sequence != null">
                AND sequence > #{sequence}
            </if>
        </where>
    </select>


</mapper>
