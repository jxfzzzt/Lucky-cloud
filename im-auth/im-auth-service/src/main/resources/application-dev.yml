server:
  compression:
    # http响应的gzip压缩
    enabled: true
    # 压缩所需的最小长度，响应长度大于等于该值就会压缩，默认2KB
    min-response-size: 2048
    # 需要压缩的响应的Content-type
    mime-types:
      - image/png
      - image/jpeg
      - image/jpg
      - text/html
      - application/javascript
      - text/css
      - application/octet-stream
      - application/json

spring:
  jackson:
    time-zone: UTC
  banner:
    location: classpath:banner.txt
  threads:
    virtual:
      enabled: true  # 开启虚拟线程
  servlet:
    multipart:
      max-file-size: 10MB
  data:
    redis:
      host: localhost
      port: 6379
      password:
      username:
      database: 0      # 数据库索引（默认为0）
      timeout: 1800000 # 连接超时时间（毫秒）
      lettuce:
        pool:
          max-active: 20  # 连接池最大连接数
          max-wait: -1    # 最大阻塞等待时间（负数表示无限制）
          max-idle: 5     # 最大空闲连接数
          min-idle: 0     # 最小空闲连接数

#springdoc相关配置
springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
  api-docs:
    enabled: true
    path: /v3/api-docs
  group-configs:
    - group: 'auth'
      paths-to-match: '/**'
      packages-to-scan: com.xy.lucky.auth  #改成你自己的包名，一般到启动类的包名

# TODO: knife4j相关配置 可以不用改
knife4j:
  enable: true        # 开启增强配置
  production: false   # 开启生产环境屏蔽
  setting:
    language: zh_cn   # 默认中文
    swagger-model-name: 实体类

# ResponseHandler 配置：排除不需要进行 Result 封装的路径
response:
  handler:
    exclude-paths:
      - /swagger-resources/**
      - /v*/api-docs/**
      - /webjars/**
      - /doc.html
      - /favicon.ico
      - /swagger-ui/**
      - /swagger-ui.html

# TODO: security
security:
  enabled: true
  crypto:
    enabled: false  # 临时禁用加密功能以排查问题
    default-encrypt: AES
    default-decrypt: AES
    aes-key: g64fi7uihebib6d34njcbybfi565645v
    exclude-paths:
      - '/swagger-ui.html'
      - '/doc.html'
      - '/webjars/**'
      - '/swagger-resources/**'
      - '/v3/api-docs'
      - '/v3/api-docs/**'
      - '/swagger-ui/**'
      - '/favicon.ico'
      - '/.well-known/**'
  sign:
    secret: g876q3f4fiddybc7684cq87b634fg87
  rsa:
    secret: g876q3f4fiddybc7684cq87b634fg87  # 可选，用于确定性生成
    data-id: im-rsa-keys.yml
    refresh-enabled: true
    check-interval: 1800000 # 单位：毫秒
    key-ttl: 720  # 单位：小时
    key-type: yaml
  qrcode:
    default-width: 300
    default-height: 300
    format: PNG
    error-correction-level: L
  oauth2:
    authorization-code-ttl-seconds: 300
    pkce-required: true
    clients:
      - client-id: mobile-app
        redirect-uris:
          - 'myapp://oauth/callback'
        scopes:
          - 'read'
          - 'write'
        require-pkce: true
  auth:
    expiration: 30                                                      #JWT过期时间 单位：min
    refresh-expiration: 168                                             #refreshToken 过期时间 单位：hour
    reuse-refresh-tokens: true
    secret: 'dasfdffdsfgfadgfshfghfgdsfgxcxfdghhfh'                     #JWT盐值
    ignore:
      - '/api/v1/auth/login'
      - '/api/v1/auth/encrypt'
      - '/api/v1/auth/publickey'
      - '/api/v1/auth/qrcode'
      - '/api/v1/auth/qrcode/status'
      - '/api/v1/auth/password'
      - '/api/v1/auth/refresh/token'
      - '/api/v1/auth/sms'
      - '/api/*/oauth2/token'
      - '/doc.html'
      - '/swagger-ui.html'
      - '/webjars/**'
      - '/swagger-resources/**'
      - '/v2/**'
      - '/v3/**'
      - '/v3/api-docs/**'
      - '/swagger-ui/**'
      - '/favicon.ico'
      - '/.well-known/**'


# TODO: log settings
logging:
  config: classpath:logback-plus.xml  #日志配置文件，会在当前项目下去找日志配置文件
  level:
    com.xy.lucky.*: info
    org.springframework: warn

# TODO 榛子云短信服务
sms:
  apiUrl: https://sms_developer.zhenzikj.com
  appId: 112181
  appSecret: 86813b86-b431-46ea-bd55-fcf63c2990a3
  code:
    ttl: 5m
    maxRetry: 3
    rateLimit:
      window: 1m
      phoneLimit: 3
      ipLimit: 20
      deviceLimit: 10
      blockDuration: 10m